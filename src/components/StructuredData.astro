---
export interface Props {
  type: "BlogPosting" | "Person" | "WebSite";
  data: any;
}

const { type, data } = Astro.props;

let structuredData = {};

if (type === "BlogPosting") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline: data.title,
    description: data.description,
    author: {
      "@type": "Person",
      name: data.author || "Manoj Mahapatra",
      url: "https://manojmahapatra.github.io/about",
    },
    datePublished: data.pubDatetime.toISOString(),
    dateModified: data.modDatetime?.toISOString() || data.pubDatetime.toISOString(),
    publisher: {
      "@type": "Person",
      name: "Manoj Mahapatra",
    },
    mainEntityOfPage: {
      "@type": "WebPage",
      "@id": data.url,
    },
    image: data.ogImage,
    articleSection: data.tags?.[0] || "Technology",
    keywords: data.tags?.join(", ") || "",
    wordCount: data.wordCount,
    timeRequired: data.readingTime,
  };
} else if (type === "Person") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "Person",
    name: "Manoj Mahapatra",
    url: "https://manojmahapatra.github.io",
    sameAs: [
      "https://github.com/manojmahapatra",
      "https://twitter.com/mahapmanoj",
      "https://www.linkedin.com/in/manojmahapatra/",
    ],
    jobTitle: "Software Engineer",
    description:
      "AI-powered tools from Swift roots to web frontiers. Every commit lands on GitHub for you to fork & remix.",
  };
} else if (type === "WebSite") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: "Manoj Mahapatra",
    url: "https://manojmahapatra.github.io",
    description:
      "iOS Engineer. Swift, Bazel, Monorepos. Writing about iOS development and build systems.",
    author: {
      "@type": "Person",
      name: "Manoj Mahapatra",
    },
    potentialAction: {
      "@type": "SearchAction",
      target: "https://manojmahapatra.github.io/search?q={search_term_string}",
      "query-input": "required name=search_term_string",
    },
  };
}
---

<script type="application/ld+json" set:html={JSON.stringify(structuredData)} />